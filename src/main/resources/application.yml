server:
  servlet:
    context-path: /api
  port: 8080

spring:
  profiles:
    active: dev
    include: jwt, bcrypt, redis
    group:
      dev: dev-db
      test: test-db

logging.level:
  org.hibernate.SQL: debug

logging:
  config: classpath:application-log-dev.yml